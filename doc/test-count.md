# Test inventory

The current suite contains unit and integration scenarios for algorithms, manifests, scanning, source adapters, profiles, storage, output artifacts, signatures, localization, and first-run configuration.

The authoritative count is the number reported by `dart test` in CI. The documentation does not hard-code a count so that adding a regression test cannot make the repository documentation stale.
