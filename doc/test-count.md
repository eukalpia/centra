# Test inventory

The current suite contains unit and integration scenarios for algorithms, manifests, scanning, source adapters, profiles, storage, output artifacts, signatures, localization, and first-run configuration.

The authoritative count is the number reported by `dart test` in CI. Documentation does not hard-code that count, so adding a regression test cannot silently make the repository description stale.
